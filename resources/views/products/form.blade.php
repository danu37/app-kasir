@extends('layouts.app')
@section('content')
<div class="page-head"><div><p class="muted">{{ $product->exists ? 'Perbarui informasi produk' : 'Tambahkan produk baru ke inventori' }}</p></div><a class="btn outline" href="{{ route('products.index') }}"><i class="ti ti-arrow-left"></i>Kembali</a></div><div class="form-panel"><form method="POST" action="{{ $product->exists ? route('products.update', $product) : route('products.store') }}">@csrf @if($product->exists) @method('PUT') @endif<div class="form-grid"><label>Nama Barang<input name="nama_barang" value="{{ old('nama_barang', $product->nama_barang) }}" required></label><label>SKU<input name="sku" value="{{ old('sku', $product->sku) }}" placeholder="Contoh: SKU-001"></label><label>Harga Jual<input type="number" name="harga" value="{{ old('harga', $product->harga) }}" min="0" required></label><label>Stok<input type="number" name="stok" value="{{ old('stok', $product->stok ?? 0) }}" min="0" required></label><label>Kategori<select name="kategori_id"><option value="">Tanpa kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('kategori_id', $product->kategori_id) == $category->id)>{{ $category->nama_kategori }}</option>@endforeach</select></label><label class="full">Deskripsi<textarea name="deskripsi" rows="4">{{ old('deskripsi', $product->deskripsi) }}</textarea></label></div><div class="form-footer"><a class="btn outline" href="{{ route('products.index') }}">Batal</a><button class="btn primary" type="submit"><i class="ti ti-device-floppy"></i>Simpan Barang</button></div></form></div>
@endsection
