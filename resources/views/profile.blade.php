@extends('layouts.app')
@section('content')
<div class="page-head"><div><p class="muted">Kelola informasi akun dan keamanan Anda</p></div></div><div class="two-col profile-grid"><div class="form-panel"><div class="profile-hero"><span class="avatar large">{{ strtoupper(substr(auth()->user()->name, 0, 2)) }}</span><div><h2>{{ auth()->user()->name }}</h2><span class="badge info">{{ ucfirst(auth()->user()->role) }}</span><p class="muted">{{ $transactionCount }} transaksi tercatat</p></div></div><h2>Informasi Akun</h2><form method="POST" action="{{ route('profile.update') }}">@csrf @method('PUT')<div class="form-grid"><label>Nama Lengkap<input name="name" value="{{ old('name', auth()->user()->name) }}" required></label><label>Username<input name="username" value="{{ old('username', auth()->user()->username) }}" required></label><label class="full">Email<input type="email" name="email" value="{{ old('email', auth()->user()->email) }}"></label></div><button class="btn primary">Simpan Profil</button></form></div><div class="form-panel"><h2>Ubah Password</h2><p class="muted">Gunakan minimal 8 karakter untuk password baru.</p><form method="POST" action="{{ route('profile.password') }}">@csrf @method('PUT')<label>Password Saat Ini<input type="password" name="current_password" required></label><label>Password Baru<input type="password" name="password" required></label><label>Konfirmasi Password<input type="password" name="password_confirmation" required></label><button class="btn primary">Perbarui Password</button></form></div></div>
@endsection
